package com.neighbor.userservice.constraints.legends;

//not being used yet, I couldnt implement the interface for the enums

public interface ValidationEnum {

    public Boolean found(String label);
}
